version: 2.1

jobs:
  test:
    macos:
      xcode: 13.4.1
    steps:
      - checkout
      - run:
          name: Run tests
          command: echo Hello World!

workflows:
    test-workflow:
      jobs:
        - test
        
        
# xcodebuild test -scheme Circle-Ci-Test-App -destination 'platform=iOS Simulator,name=iPhone 12,OS=15.5' -configuration 'Debug' 
